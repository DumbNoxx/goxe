# yaml-language-server: $schema=https://taskfile.dev/schema.json

version: '3'

tasks:
  dev:
    desc: Run the app in development mode
    cmds:
      - echo "Running the application"
      - go run ./cmd/goxe .
    silent: true
  dev#:
    desc: Run the app in development mode with flags or arguments
    cmds:
      - echo "Running the application with flags or arguments"
      - go run ./cmd/goxe {{.CLI_ARGS}}
    silent: true
  build:
    desc: Building program in golang
    cmds:
      - echo "Built application in /bin"
      - go build -o bin/goxe ./cmd/goxe 
    silent: true 
  test:
    desc: Run testing application
    cmds:
      - echo "Running test"
      - go test -bench=. -benchmem ./cmd/goxe
    silent: true
